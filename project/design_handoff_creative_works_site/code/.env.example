# Environment variables for Creative Works (.env.local)
# Copy to .env.local and fill in real values. Never commit real keys.

# Stripe — from your Stripe dashboard
STRIPE_SECRET_KEY=sk_test_xxx
STRIPE_WEBHOOK_SECRET=whsec_xxx
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_xxx

# Site URL — used to build Stripe success/cancel URLs
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Contact form delivery (choose one integration; wire in the contact API route)
# RESEND_API_KEY=
# SENDGRID_API_KEY=
# CONTACT_FORM_WEBHOOK_URL=
